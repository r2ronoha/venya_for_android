<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/venya_table_title_cell</item>
        <item name="android:popupWindowStyle">@style/VenyaPopUpMenu</item>
    </style>
    <!-- Base application theme. -->
    <style name="VenyaPopUpMenu" parent="Widget.AppCompat.Light.PopupMenu">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/venya_menu</item>
    </style>

</resources>
